---
title: 'Circle Member'
description: 'Represents a membership relationship between a member and a circle, tracking association status and history.'
entity: 'circle_member'
category: 'circles-roles'
---

## Fields

<EntityFields
  fields={[
    {
      name: 'id',
      type: 'uuid',
      description: 'Unique identifier for the circle membership',
    },
    { name: 'circleId', type: 'uuid', description: 'Reference to the circle' },
    { name: 'memberId', type: 'uuid', description: 'Reference to the member' },
    {
      name: 'createdAt',
      type: 'Timestamp',
      description: 'When the membership was created',
    },
    {
      name: 'archived',
      type: 'Boolean',
      description: 'Whether the membership is archived (default: false)',
    },
  ]}
/>

## Relationships

**Object Relationships:**

- `circle` — The circle this membership belongs to
- `member` — The member associated with this circle membership

## Query Examples

### Get Members of a Specific Circle

```graphql
query GetCircleMembers($circleId: uuid!) {
  circle_member(
    where: { circleId: { _eq: $circleId }, archived: { _eq: false } }
  ) {
    id
    circle {
      id
      role {
        name
      }
    }
    member {
      id
      name
      description
    }
    createdAt
  }
}
```

## Mutation Examples

### Add a Member to a Circle

```graphql
mutation AddCircleMember {
  insert_circle_member_one(
    object: { circleId: "circle-id", memberId: "member-id" }
  ) {
    id
    circle {
      id
      role {
        name
      }
    }
    member {
      name
    }
  }
}
```

### Archive a Circle Membership

```graphql
mutation UpdateCircleMember {
  update_circle_member_by_pk(
    pk_columns: { id: "circle-member-id" }
    _set: { archived: true }
  ) {
    id
    archived
  }
}
```

## Permissions

- **Organization owners and admins:** Can add and remove members from any circle
- **Circle leaders:** Can manage members in their circles
- **Members:** Can view circle memberships in circles they belong to
- A member cannot be added to a circle if they already have an active membership

<Callout type="info" title="Notes">
  The `archived` flag preserves membership history while marking inactive
  memberships. Circle membership determines access to circle resources like
  meetings, tasks, and decisions. Members can belong to multiple circles
  simultaneously.
</Callout>
