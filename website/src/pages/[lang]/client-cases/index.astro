---
import ClientCaseIndex from '../../../layouts/ClientCaseIndex.astro'
import config from '../../../../website.config'

export function getStaticPaths() {
  return config.langs.map(lang => ({ params: { lang } }))
}

const { lang } = Astro.params
---

<ClientCaseIndex lang={lang} />
