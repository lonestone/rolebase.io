[build]
  command = "npm run build"
  publish = "dist"

# Redirect root to browser language
[[redirects]]
  from = "/"
  to = "/fr"
  status = 302
  conditions = { Language = ["fr"] }

[[redirects]]
  from = "/"
  to = "/en"
  status = 302
