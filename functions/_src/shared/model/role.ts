// Value of role property "link"
// Link to another circle (parent or other)
// to represent parent circle's purpose in this other circle.
//   string = circleId
//   "Parent" = grand parent circle
//   "No" = not a link
export enum RoleLink {
  No = 'No',
  Parent = 'Parent',
}
