---
title: "News"
description: "A unified view of recent activities aggregating active threads, decisions, and completed meetings. Provides a centralized activity feed across an organization."
entity: "news"
---
import EntityFields from '../../../components/EntityFields.astro'
import Callout from '../../../components/Callout.astro'

## Fields

<EntityFields fields={[
  { name: "id", type: "uuid", description: "Unique identifier for the news item (auto-generated)" },
  { name: "orgId", type: "uuid", description: "Reference to the organization" },
  { name: "circleId", type: "uuid", description: "Reference to the circle (optional)" },
  { name: "threadId", type: "uuid", description: "Reference to the source thread (if applicable)" },
  { name: "decisionId", type: "uuid", description: "Reference to the source decision (if applicable)" },
  { name: "meetingId", type: "uuid", description: "Reference to the source meeting (if applicable)" },
  { name: "createdAt", type: "Timestamp", description: "When the news item was created" },
]} />

## Implementation

The news entity is implemented as a SQL view that combines:

1. **Active threads** — Threads with status "Active" or "Preparation" that are not archived
2. **Non-archived decisions** — All decisions that have not been archived
3. **Completed meetings** — Meetings that have ended and are not archived

## Relationships

### Object Relationships

- `org` — The organization this news item belongs to
- `circle` — The circle associated with this news item (if any)
- `thread` — The source thread (if news is from a thread)
- `decision` — The source decision (if news is from a decision)
- `meeting` — The source meeting (if news is from a meeting)

## Query Examples

### List Recent News

```graphql
query GetRecentNews($orgId: uuid!) {
  news(
    where: { orgId: { _eq: $orgId } }
    order_by: { createdAt: desc }
    limit: 10
  ) {
    id
    createdAt
    thread {
      title
      status
    }
    decision {
      title
      description
    }
    meeting {
      title
      summary
    }
    circle {
      role {
        name
      }
    }
  }
}
```

### Query News with Filters and Aggregation

```graphql
query GetFilteredNews($where: news_bool_exp!) {
  news(
    where: { _and: [{ orgId: { _eq: $orgId } }, $where] }
    order_by: { createdAt: desc }
  ) {
    id
    createdAt
    threadId
    decisionId
    meetingId
    org {
      name
    }
  }
  news_aggregate(where: { _and: [{ orgId: { _eq: $orgId } }, $where] }) {
    aggregate {
      count
    }
  }
}
```

## Permissions

News access is controlled based on the following rules:

- Organization members can view news items from their organization
- News items inherit visibility rules from their source entities (thread, decision, and meeting privacy settings)
- Public access is limited to organizations with shared information

<Callout type="info">
News items are automatically generated from source entities. Each news item represents exactly one source (a thread, decision, or meeting). This entity is read-only since it is a SQL view.
</Callout>
