---
title: "Fichier"
description: "Représente un fichier stocké dans Rolebase. Les fichiers peuvent être associés à des organisations et des membres pour des usages tels que les photos de profil, le stockage de documents et les pièces jointes."
entity: "file"
---
import EntityFields from '../../../components/EntityFields.astro'
import Callout from '../../../components/Callout.astro'

## Champs

<EntityFields fields={[
  { name: "id", type: "uuid", description: "Identifiant unique du fichier (généré automatiquement)" },
  { name: "bucketId", type: "String", description: "Référence au bucket de stockage" },
  { name: "name", type: "String", description: "Nom original du fichier (optionnel)" },
  { name: "mimeType", type: "String", description: "Type MIME du fichier (optionnel)" },
  { name: "size", type: "Integer", description: "Taille du fichier en octets (optionnel)" },
  { name: "etag", type: "String", description: "Balise d'entité pour la mise en cache et les requêtes conditionnelles (optionnel)" },
  { name: "isUploaded", type: "Boolean", description: "Indique si le fichier a été entièrement téléversé (optionnel)" },
  { name: "metadata", type: "JSON", description: "Métadonnées supplémentaires sur le fichier (optionnel)" },
  { name: "createdAt", type: "Timestamp", description: "Date de création du fichier (par défaut : horodatage actuel)" },
  { name: "updatedAt", type: "Timestamp", description: "Date de dernière mise à jour du fichier (par défaut : horodatage actuel)" },
  { name: "uploadedByUserId", type: "uuid", description: "Référence à l'utilisateur qui a téléversé le fichier (optionnel)" },
]} />

## Relations

### Relations objet

- `bucket` — Le bucket de stockage contenant le fichier

### Relations tableau

- `members` — Membres utilisant ce fichier (ex. : comme photo de profil)
- `orgs` — Organisations associées à ce fichier

## Buckets de stockage

Les fichiers sont stockés dans des buckets qui définissent les paramètres de stockage :

- `cacheControl` — Paramètres de contrôle du cache
- `downloadExpiration` — Durée d'expiration du lien de téléchargement
- `maxUploadFileSize` — Taille maximale autorisée pour les fichiers
- `minUploadFileSize` — Taille minimale autorisée pour les fichiers
- `presignedUrlsEnabled` — Indique si les URL pré-signées sont activées pour un accès temporaire sécurisé

## Exemples de requêtes

### Obtenir un fichier spécifique

```graphql
query GetFile($id: uuid!) {
  file_by_pk(id: $id) {
    id
    name
    mimeType
    size
    createdAt
    metadata
  }
}
```

## Permissions

L'accès aux fichiers est contrôlé par plusieurs facteurs :

- **Appartenance à l'organisation** — Les membres peuvent accéder aux fichiers associés à leur organisation
- **Rôle du membre** — Les droits de téléversement et de modification dépendent du rôle du membre
- **Permissions du bucket** — La configuration du bucket de stockage régit l'accès et les limites de taille
- **Droits du téléverseur** — Les métadonnées du fichier peuvent être mises à jour par le téléverseur original
- **Analyse antivirus** — Les fichiers téléversés sont analysés pour la sécurité

<Callout type="info">
Les fichiers sont stockés de manière sécurisée dans des buckets de stockage configurables. Les URL pré-signées permettent un accès temporaire sécurisé, et la validation du type MIME assure un traitement correct des fichiers. Les ETags supportent la mise en cache et les requêtes conditionnelles pour une livraison efficace des fichiers.
</Callout>
