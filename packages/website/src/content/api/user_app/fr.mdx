---
title: "Application utilisateur"
description: "Représente une application ou intégration connectée d'un utilisateur dans Rolebase. Suit les paramètres d'application, les préférences et les détails d'authentification pour les services tiers comme Google Calendar et Microsoft 365."
entity: "user_app"
---
import EntityFields from '../../../components/EntityFields.astro'
import Callout from '../../../components/Callout.astro'

## Champs

<EntityFields fields={[
  { name: "id", type: "uuid", description: "Identifiant unique de la connexion d'application (généré automatiquement)" },
  { name: "userId", type: "uuid", description: "Référence à l'utilisateur" },
  { name: "type", type: "Text", description: "Type d'application : GoogleCalendar ou Office365" },
  { name: "config", type: "JSON", description: "Configuration publique de l'application" },
  { name: "secretConfig", type: "JSON", description: "Configuration sensible de l'application (ex. : jetons)" },
  { name: "tmpData", type: "JSON", description: "Stockage de données temporaires (optionnel)" },
  { name: "createdAt", type: "Timestamp", description: "Date de connexion de l'application (par défaut : horodatage actuel)" },
]} />

## Types d'application

Le champ `type` accepte les valeurs suivantes :

- **GoogleCalendar** — Intégration Google Calendar
- **Office365** — Intégration Microsoft 365 Calendar

## Relations

### Relations objet

- `user` — L'utilisateur auquel cette connexion d'application appartient

## Exemples de requêtes

### Lister les applications utilisateur

```graphql
query GetUserApps {
  user_app(order_by: { createdAt: desc }) {
    id
    type
    config
    createdAt
    userId
  }
}
```

## Permissions

L'accès aux connexions d'applications est restreint à l'utilisateur propriétaire :

- Les utilisateurs peuvent consulter leurs propres connexions d'applications (limité aux champs `config`, `id`, `type` et `userId`)

<Callout type="info">
Chaque utilisateur peut avoir plusieurs connexions d'applications, mais il existe une contrainte d'unicité sur la paire (`userId`, `type`) -- ce qui signifie une connexion par type d'application par utilisateur. Le champ `secretConfig` stocke des données sensibles comme les jetons OAuth et n'est pas exposé dans les requêtes.
</Callout>
