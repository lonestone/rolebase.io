---
import { getLocaleFromPath, translations } from '../i18n'

interface Field {
  name: string
  type: string
  description: string
}

interface Props {
  fields: Field[]
}

const { fields } = Astro.props
const locale = getLocaleFromPath(Astro.url.pathname)
const t = translations[locale]
---

<table class="my-8 w-full border-collapse text-sm">
  <thead>
    <tr>
      <th class="border-b-2 border-gray-200 bg-gray-50 px-5 py-3 text-left text-base font-semibold">{t.fieldLabel}</th>
      <th class="border-b-2 border-gray-200 bg-gray-50 px-5 py-3 text-left text-base font-semibold">{t.typeLabel}</th>
      <th class="border-b-2 border-gray-200 bg-gray-50 px-5 py-3 text-left text-base font-semibold">{t.descriptionLabel}</th>
    </tr>
  </thead>
  <tbody>
    {fields.map(field => (
      <tr>
        <td class="border-b border-gray-100 px-5 py-3 text-base"><code class="text-sm whitespace-nowrap">{field.name}</code></td>
        <td class="border-b border-gray-100 px-5 py-3 text-base"><code class="text-sm whitespace-nowrap">{field.type}</code></td>
        <td class="border-b border-gray-100 px-5 py-3 text-base">{field.description}</td>
      </tr>
    ))}
  </tbody>
</table>
