---
interface Props {
  icon: string
  alt: string
}

const { icon, alt } = Astro.props
---

<div class="flex flex-col items-center gap-3">
  <img src={icon} alt={alt} class="h-16" />
  <p class="mb-0"><slot /></p>
</div>
