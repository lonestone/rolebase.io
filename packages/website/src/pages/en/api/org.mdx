---
---

import ApiReference from '../../../components/ApiReference.astro'
import EntityFields from '../../../components/EntityFields.astro'
import Callout from '../../../components/Callout.astro'

<ApiReference title="Organization" entity="org" description="The top-level entity that contains all other entities such as circles, roles, members, meetings, and tasks.">

## Fields

<EntityFields fields={[
  { name: "id", type: "uuid", description: "Unique identifier for the organization" },
  { name: "name", type: "String", description: "Name of the organization" },
  { name: "slug", type: "String", description: "URL-friendly unique identifier (optional)" },
  { name: "archived", type: "Boolean", description: "Whether the organization is archived (default: false)" },
  { name: "createdAt", type: "Timestamp", description: "When the organization was created" },
  { name: "defaultGraphView", type: "String", description: "Default graph view setting (optional)" },
  { name: "protectGovernance", type: "Boolean", description: "Whether governance changes are restricted to leaders (default: false)" },
  { name: "shareMembers", type: "Boolean", description: "Whether member information is publicly shared (default: false)" },
  { name: "shareOrg", type: "Boolean", description: "Whether organization information is publicly shared (default: false)" },
]} />

## Relationships

**One-to-One:**
- `org_subscription` — The organization's subscription information

**One-to-Many:**
- `circles` — Organizational circles
- `decisions` — Organization decisions
- `files` — Organization files
- `logs` — Activity logs
- `meeting_templates` — Meeting templates
- `meetings` — Meetings
- `meetings_recurring` — Recurring meetings
- `members` — Organization members
- `news` — News items
- `roles` — Organization roles
- `task_views` — Task views
- `tasks` — Tasks
- `threads` — Discussion threads

## Query Examples

### Get a Specific Organization

```graphql
query GetOrganization($id: uuid!) {
  org_by_pk(id: $id) {
    id
    name
    slug
    archived
    createdAt
    members {
      id
      name
      role
    }
    circles {
      id
      name
    }
  }
}
```

## Mutation Examples

### Update an Organization

```graphql
mutation UpdateOrganization {
  update_org_by_pk(
    pk_columns: { id: "your-org-id" }
    _set: {
      name: "New Organization Name"
      shareMembers: true
      protectGovernance: true
    }
  ) {
    id
    name
    shareMembers
    protectGovernance
  }
}
```

## Permissions

- **Owner:** Full access to all organization settings and data
- **Admin:** Can manage most organization settings and data
- **Member:** Can view organization data based on sharing settings

<Callout type="info" title="Notes">
  The `slug` field must be unique across all organizations. When `protectGovernance` is enabled, only circle leaders can modify governance-related entities. The `shareMembers` and `shareOrg` flags control visibility of organization data to external users.
</Callout>

</ApiReference>
