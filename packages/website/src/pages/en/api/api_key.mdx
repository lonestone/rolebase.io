---
---

import ApiReference from '../../../components/ApiReference.astro'
import EntityFields from '../../../components/EntityFields.astro'
import Callout from '../../../components/Callout.astro'

<ApiReference title="API Key" entity="api_key" description="Represents an API key for programmatic access to the Rolebase API. API keys authenticate and authorize integration with external applications and services.">

## Fields

<EntityFields fields={[
  { name: "id", type: "uuid", description: "Unique identifier for the API key (auto-generated)" },
  { name: "userId", type: "uuid", description: "Reference to the user who owns this key" },
  { name: "name", type: "String", description: "Descriptive name for the API key" },
  { name: "value", type: "String", description: "The actual API key value (auto-generated, shown once on creation)" },
  { name: "createdAt", type: "Timestamp", description: "When the API key was created (defaults to current timestamp)" },
]} />

## Relationships

### Object Relationships

- `user` — The user this API key belongs to

## Query Examples

### List API Keys

```graphql
query apiKeys($userId: uuid!) {
  api_key(where: { userId: { _eq: $userId } }) {
    id
    name
    value
    createdAt
  }
}
```

## Mutation Examples

### Create an API Key

```graphql
mutation createApiKey($userId: uuid!, $name: String!) {
  insert_api_key_one(object: { userId: $userId, name: $name }) {
    id
    name
    value
    createdAt
  }
}
```

### Rename an API Key

```graphql
mutation renameApiKey($id: uuid!, $name: String!) {
  update_api_key_by_pk(pk_columns: { id: $id }, _set: { name: $name }) {
    id
    name
    value
    createdAt
  }
}
```

### Delete an API Key

```graphql
mutation deleteApiKey($id: uuid!) {
  delete_api_key_by_pk(id: $id) {
    id
  }
}
```

## Permissions

API key management is restricted to the owning user:

- Users can create, view, rename, and delete their own API keys only

<Callout type="info">
The key `value` is only shown once upon creation — store it securely. Best practices include regularly rotating keys, monitoring usage, deleting unused keys, and never sharing them publicly.
</Callout>

</ApiReference>
