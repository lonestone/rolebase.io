CREATE TABLE "public"."log" ("id" uuid NOT NULL DEFAULT gen_random_uuid(), "orgId" uuid NOT NULL, "userId" uuid NOT NULL, "memberId" uuid NOT NULL, "memberName" text NOT NULL DEFAULT '''::text', "meetingId" uuid, "createdAt" timestamptz NOT NULL DEFAULT now(), "display" json NOT NULL, "changes" json NOT NULL, "cancelLogId" uuid, "cancelMemberId" uuid NOT NULL, "cancelMemberName" uuid NOT NULL, "canceled" bool NOT NULL DEFAULT false, PRIMARY KEY ("id") , FOREIGN KEY ("orgId") REFERENCES "public"."org"("id") ON UPDATE restrict ON DELETE cascade, FOREIGN KEY ("userId") REFERENCES "auth"."users"("id") ON UPDATE restrict ON DELETE cascade, FOREIGN KEY ("id") REFERENCES "public"."member"("id") ON UPDATE restrict ON DELETE cascade);
CREATE EXTENSION IF NOT EXISTS pgcrypto;
